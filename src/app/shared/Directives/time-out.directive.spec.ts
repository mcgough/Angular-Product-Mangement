import { TimeOutDirective } from './time-out.directive';

describe('TimeOutDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeOutDirective();
    expect(directive).toBeTruthy();
  });
});
